import { FC } from "react";
import { Container } from "../ui/Container";

interface Props {
}

export const Faxing: FC<Props> = ({ }) => {
  return (
      <Container title="Faxing">
        <div className="mt-2 ml-8">Faxing</div>
      </Container>
  );
};
